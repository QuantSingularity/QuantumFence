[pytest]
asyncio_mode = strict
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires DB or external services)
    api: API-level tests (requires running app or test client)